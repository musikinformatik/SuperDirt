d1 $ "bd*8"
once $ "bd"
:!ls ..
:!pwd


loadSoundFiles path = changeFunc' tidal list
   where list = [("scMessage",VS "loadSoundFiles"),("filePath",VS path)]

loadSoundFileFolder path = changeFunc' tidal list
   where list = [("scMessage",VS "loadSoundFileFolder"),("filePath",VS path)]

loadSoundFiles "/M/tidalSamples/unsorted_wavs/"




-- :!pwd
-- :script TidalFiles/FuncSend.hs 
-- TidalFiles/TidalInterface.hs


d2 $ fast 2 $ n "<1 0 0 0> ~ ~ 0 ~ ~ 0 ~"  # "808bd(3,8)" # gain 1.5 # orbit 0
      # lpf 800

d1 $ fast 2 $ hurry (2/2) $ (# n "[-6 -12]/2") $ loopAt 1 $ slice 16 "0 .. 7" 
    $ "unsorted_wavs" 
    # room 0.2 # shape 0.4 # sus 0.5

$ fast 2 $ slice 17 "[9(1,8) 4*4]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2

do
  hush
  all $ id
  -- all $ (linger (1/6))
  d1 $ fast 2 $ cat 
      [ struct "<t f> ~ ~ t*<0!3 0>" $ "[future,808bd:1]" # sus 0.9 # speed 0.8
      --   -- , superimpose (# speed 1.2) $ "[snare,sd:4,tabla]" # sus 0.2 
        ,slice 13 "[9!3 7*2]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2
        ,slice 17 "[12*4 7 7 6]" $ "unsorted_wavs:-5" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2
            # speed 1.5
            -- # comb 0.2
        ,slice 17 "[9 10 12]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2
        -- ,slice 30 "[0 1 2 3]*2" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.3 # speed 0.5
        --     # hpf 200
        -- , superimpose (# speed 2.2) $ "<[snare,sd:4,tabla] [[snare,sd:4,tabla] future ~ [~ future*<1 2> ~ ~]]>" 
        --     # sus 0.2 
            ] # distort 0.1 # gain 1.2
  d2 $ (0.2 <~) $ superimpose (chop 1 . (hurry 1) . (#sus 0.05) .(# cut 0) ) $ (|+ begin "[<0.07> 0.1]*2" )$ fast 2 $ slice 13 "[9 7 8 7]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4
        # speed 0.75 # gain 1.2
        # cps (90/120)

do
  hush
  all $ (|*| speed "<1.2!3 [1.5!3 <1.5 0.75>]>")
  all $ id
  d1 $ fast 2 $ cat 
      [ "[future,808bd:1]" # sus 0.9
        , superimpose (# speed 1.2) $ "[snare,sd:4,tabla]" # sus 0.2 
        ,slice 13 "[9 9 9 8]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2
        , superimpose (# speed 1.2) $ "<[snare,sd:4,tabla] [[snare,sd:4,tabla] future ~ [~ future*<1 2> ~ ~]]>" 
            # sus 0.2 ] # distort 0.1 # gain 1.2
  d2 $ often (stut "[2 | 4]" (3/4) (1/32) )
      $ (0.2 <~) $ superimpose (chop 1 . (hurry 1) . (#sus 0.05) .(# cut 0) ) $ (|+ begin "[0.01 0.1]*2" )$ fast 2 $ slice 13 "[9 7 8 7]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4
        # speed 0.75 # gain 1.2 # cps (90/120)


do
  hush
  -- all $ (|*| speed "<1.2!3 [1.5!3 <1.5 0.75>]>")
  all $ (linger (1/1) . ((hurry ("[2 2 2 <3 4 2 2 >]"/2))) . (# speed 1))
  d1 $ fast 2 $ cat 
      [ "[future,808bd:1]" # sus 0.9
        , superimpose (# speed 1.2) $ "[snare,sd:4,tabla]" # sus 0.2 
        ,slice 13 "[9 9 9 8]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4 # sus 0.2
        , superimpose (# speed 1.2) $ "<[snare,sd:4,tabla] [[snare,sd:4,tabla] future ~ [~ future*<1 2> ~ ~]]>" 
            # sus 0.2 ] # distort 0.1 # gain 1.2
  d2 $ often (stut "[2 | 4]" (3/4) (1/32) )
      $ (0.2 <~) $ superimpose (chop 1 . (hurry 1) . (#sus 0.05) .(# cut 0) ) $ (|+ begin "[0.01 0.1]*2" )$ fast 2 $ slice 13 "[9 7 8 7]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4
        # speed 0.75 # gain 1.2 # cps (90/120)



do
  hush
  all $ id
  d1 $ speed ("~ ~ 2 1" /2)# "sd" # gain 2

superimpose (# speed 1.2) $ "[snare,sd:4,tabla]" # sus 0.2 
        ,slice 12 "[9*4]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4
        ,slice 12 "[9*4]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4
        ,slice 12 "[9*4]" $ "unsorted_wavs:-4" # cut "-1" # room 0.2 # shape 0.4

-- test pat
hush